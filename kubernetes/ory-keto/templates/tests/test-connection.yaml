apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ory-keto.fullname" . }}-test-connection"
  labels:
    {{- include "ory-keto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ory-keto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
