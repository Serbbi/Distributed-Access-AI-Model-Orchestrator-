apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ory-kratos.fullname" . }}-test-connection"
  labels:
    {{- include "ory-kratos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ory-kratos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
